# Copy this file to ".env" on the server and fill in real secrets.
# docker-compose automatically reads a ".env" file in the same directory.

MYSQL_ROOT_PASSWORD=change-this-to-a-strong-password
APP_JWT_SECRET=change-this-to-a-long-random-string-at-least-32-chars
